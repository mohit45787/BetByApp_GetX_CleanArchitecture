import 'package:get/get.dart';

class AppDimen {
  static double screenWidth = Get.width;
  static double screenHeight = Get.height;

  var blur = 100.0;

  var colorOpacity = 0.03;

  /// unit dp for font size
  static double unitWidth = screenWidth * 0.0025;
  static double unitHeight = screenHeight * 0.001875;
}
